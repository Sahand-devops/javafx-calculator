package com.friends.operators;

public class ExponentiationOperator {
    public static double calculateExponentiation(double base, double exponent) {
        return Math.pow(base, exponent);
    }
}
